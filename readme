para poder correr el proyecto debes tener instalado:
nodeJS -> en su pagina oficial podras tener acceso a los distintos instaladores
TypeScript -> npm install -g typescript
Jest -> npm install jest babel-jest @babel/core @babel/preset-env @babel/preset-typescript @types/jest -D

para ejecutar el proyecto:
Primero debes instalar todas las dependencias y el node modules utilizando
    npm install
si quieres testear una solucion
    pnpm jest ejercicios/nombre_de_carpeta_a_testear
si quieres testear todo el proyecto
    pnpm test